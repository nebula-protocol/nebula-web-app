import { css } from 'styled-components';

export const walletButtonMeasure = css`
  display: inline-block;

  border-radius: 17px;
  height: 32px;
  min-height: 32px;

  font-size: 11px;

  padding: 0 17px;

  cursor: pointer;
  user-select: none;
`;
